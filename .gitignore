# pixi environments
.pixi/*
!.pixi/config.toml

# lock files
pixi.lock
pyproject.lock

# python cache
__pycache__/
*.pyc


##### pyudales #####
libs/pyudales/u-dales/*
libs/pyudales/outputs/*
libs/pyudales/experiments/*
libs/pyudales/.pixi/*
libs/pyudales/pixi.lock
libs/pyudales/pyproject.lock
libs/pyudales/__pycache__/*
libs/pyudales/*.pyc

##### pypalm #####
libs/pypalm/palm_model_system/
libs/pypalm/palm_model_system.tar.gz
libs/pypalm/__pycache__/*
libs/pypalm/*.pyc

# temp files
.temp/*

# Raw geometry exports dropped at the repo root (buildings.stl,
# groundpatched_extrude.stl, ...). These are inputs to
# tools/prepare_case_stl.py — often hundreds of MB — and are converted into the
# committed, domain-frame STL under examples/<case>/. Never commit the raw ones.
/*.stl
/*.STL

# UrbanTALES geometry dataset — bulk data (~350 MB rasters + ~600 MB STLs),
# regenerable via examples/geometries/{download_urbantales_geometries,rasters_to_stl}.py.
# The scripts stay tracked; the downloaded/derived data does not.
examples/geometries/raw/
examples/geometries/processed/

# Figures
*.pdf
*.png
*.mp4
figures/*

# torch weights
*.pt

# trained model weights
model_weights/

# macOS files
*.DS_Store

# job output files
job_scripts/delftblue/out_files/*
job_scripts/snellius/out_files/*

# training data
training_data/*
training_data_old/*

# esmda files
esmda_init_conditions/*
esmda_results/*


# cache
.cache/*

# Stray Hydra default run dir (when a script runs without the configured run.dir).
outputs/

# latex
latex/

# Ground truth simulations
ground_truth/*
ground_truth_spunup/*
ground_truth_spunup/large/*

# temp folders
.temp_udales/*
.temp_palm/*
.temp_lbm/*

# Metrics and comparison
comparison/*
sweep_metrics/*

# animations
*.gif
*.mp4

# generated artifacts
.temp
slurm-*.out
state.nc
params.nc


.local_runs/*
.temp_pyudales/*
.temp_pypalm/*
.temp_pylbm/*
.temp_pypalm_draw_a/*
.temp_pypalm_draw_b/*
.temp_pyudales_draw_a/*
.temp_pyudales_draw_b/*


# Results
results/*

# manuscripts and model comparison outputs
manuscripts/
model_comparison/

# Presentations
presentations/*
