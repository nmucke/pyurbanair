# @package filtering
# No parameter evolution between cycles. Parameter mode then requires an
# inflation option for spread maintenance (enforced at construction).
# Selected with `filtering/evolution=none` (the default).
parameter_evolution: null
