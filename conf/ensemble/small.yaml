# Sizing ensemble: small. Override with `ensemble=...` (or `ensemble.*=...`) on
# the CLI.
ensemble_size: 8
num_parallel_processes: 1
num_cpus_per_process: 1
failure:
  policy: resample_from_successes
  jitter_scale: 0.05
  seed: 0
